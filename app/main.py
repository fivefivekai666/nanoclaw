"""
app/main.py

这是项目的“最小启动入口”。

当前阶段我们故意不做复杂逻辑，
只做一件最简单的事：

    启动后打印一句话

这样做的原因是：
1. 先验证项目结构是通的
2. 先验证 pyproject.toml 的脚本入口是通的
3. 先验证 pip install -e . 安装后命令能跑
4. 后面再逐步把 main() 换成真正的 CLI / gateway 入口

记住：
从零开始做项目时，第一步不是“功能强”，
而是“启动链路先打通”。
"""


def main() -> None:
    """
    项目的命令行入口函数。

    当你在终端执行：
        myagent

    实际上 Python 会通过 pyproject.toml 中的定义，
    调用到这个函数。

    现在我们只打印一行字，
    用来确认整个项目已经被正确安装、正确识别、正确运行。
    """
    print("myagent booted")
